[custom]
; 沿用机场原规则（OpenAI/Claude 等），这里只“改分组与默认出口”
enable_rule_generator=false
overwrite_original_rules=false

; ① 上游拨号组：用正则抓常见地区命名，避免精确名对不上导致 not found
; 小白：匹配名字里含“香港/新加坡/日本/HK/SG/JP”的节点，最后给 DIRECT 兜底
custom_proxy_group=GPT_UPSTREAM`select`(香港|新加坡|日本|HK|SG|JP).*`[]DIRECT

; ② 你的固定 IP 组：匹配节点名含 chatgptself 或备注含域名 res.proxy-seller.com
; 小白：名字里只要出现这些关键字，就自动收进来；最后给 DIRECT 兜底
custom_proxy_group=chatgptself_group`select`(res\.proxy-seller\.com|.*[Cc]hatgptself.*)`[]DIRECT

; ③ 串联链（Mihomo/Meta 支持）：先走 GPT_UPSTREAM（机场上游）再走 chatgptself（你的固定 IP）
custom_proxy_group=chatgpt_chain`relay`[]GPT_UPSTREAM`[]chatgptself_group

; ④ 覆盖常见 AI 分流组名：把默认选项置顶为“串联链”（去重、去前导空格）
custom_proxy_group=🧲 OpenAI`select`[]chatgpt_chain`[]chatgptself_group`[]GPT_UPSTREAM`[]DIRECT
custom_proxy_group=OpenAI`select`[]chatgpt_chain`[]chatgptself_group`[]GPT_UPSTREAM`[]DIRECT
custom_proxy_group=ChatGPT`select`[]chatgpt_chain`[]chatgptself_group`[]GPT_UPSTREAM`[]DIRECT
custom_proxy_group=🧲 Claude`select`[]chatgpt_chain`[]chatgptself_group`[]GPT_UPSTREAM`[]DIRECT
custom_proxy_group=Claude`select`[]chatgpt_chain`[]chatgptself_group`[]GPT_UPSTREAM`[]DIRECT
custom_proxy_group=AI`select`[]chatgpt_chain`[]chatgptself_group`[]GPT_UPSTREAM`[]DIRECT
custom_proxy_group=AIGC`select`[]chatgpt_chain`[]chatgptself_group`[]GPT_UPSTREAM`[]DIRECT
custom_proxy_group=🤖 AI`select`[]chatgpt_chain`[]chatgptself_group`[]GPT_UPSTREAM`[]DIRECT
